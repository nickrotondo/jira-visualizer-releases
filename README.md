# Jira Visualizer — Releases

Public build artifacts and the auto-update feed for **Jira Visualizer**.
The application source lives in a separate private repository.

Grab the latest `.dmg` from the [Releases](../../releases) page.
